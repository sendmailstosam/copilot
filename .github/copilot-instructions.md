# Copilot Code Review Instructions

## Styling Rules

- All style changes must use Tailwind CSS utility classes.
- Do not use inline styles (e.g., `style="..."`).
- Do not use `<style>` tags.
- Do not use external CSS files.
